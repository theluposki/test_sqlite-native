import api from "./api/index.js";

export const testApi = () => {
  // console.log(api.update({table: "users", items: { email: "luposki@mail.com" }, where: { nickname: "lucas" }}));
  // console.log(api.delet({ table: "users", where: { nickname: "lucas" } }));
  //   console.log(api.select({ table: "users" }));
  //   console.log(api.selectBy({ table: 'users', where: { nickname: 'mylena' }}))
  // console.log(api.selectByLike({ table: "users", where: "nickname", like: "l%" }));
};
